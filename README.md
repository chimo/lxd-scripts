# lxd-scripts

## TODOs

Readme:

* Explain what this is, "documentation"

General:

* Config files to store which "types" profile should be applied to the
  container instead of (or as another option to) providing them to
  `create-container.sh`

