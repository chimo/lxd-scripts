# lxd-scripts

Various scripts to automate LXD management.
See READMEs in individual folders for more information.

## TODOs

* Support lxd projects (via "-p" flag). The default behaviour should list
  projects and iterate through all of them
* Automated tests

